# RECORD — ledgerline (Ledgerline)

주문 제작 4건째. **세 번 만든 첫 주문**이며, 이 기록의 값어치는 두 번의 오판이 무엇이었는지에 있다.

- 라우트: `/commissioned/ledgerline`
- 산출물: `app/src/app/commissioned/ledgerline/` (10파일)
- 레퍼런스: `reference-sequence.png` (사용자 제공, Dribbble/Sequence)
- 1차 2026-08-12 · 2차 2026-08-13 · **3차(최종) 2026-08-13**
- `works.ts` **미등재**

## 1. 세 판과 두 번의 오판

사용자가 핀테크 대시보드 목업을 주고 **그 형태를 원했다.** 세 번 만에 맞췄다.

| 판 | 무엇을 만들었나 | 왜 어긋났나 |
|---|---|---|
| **1차** | 셸 없는 전폭 1단 에디토리얼 리포트 | 내가 "골격을 바꾸자"고 권했다 |
| **2차** | `/dash/d30`을 참고한 제품 대시보드 | 셸은 생겼으나 **레퍼런스가 아니라 d30을 따라갔다** |
| **3차** | 레퍼런스 이미지를 designer가 직접 보고 제작 | — |

### 오판 ① — 적용되지 않는 규칙을 인용했다 (1차)

권한 근거 둘이 **모두 이 주문에 걸리지 않았다.**

> `dash-brief-v3:51` — "좌 사이드바 + KPI 4장 가로줄 + 8/4 차트+사이드 + 풀폭 테이블"은 하나의 아키타입일 뿐이다. **같은 세대 안에서** 구조가 겹치면 실패.

"같은 세대 안에서"는 **자율 라운드가 후보 3개를 낼 때의 다양성 규칙**이지 그 레이아웃 금지가 아니다. 같은 문서 위쪽(`:26`·`:29`)은 오히려 좌측 사이드바·상단바·8/4 그리드·KPI 4열을 **완성도 기준으로 요구한다.**

두 번째 근거 "dashboard 12작품 포화"도 `page-commission:22`에 따라 **등재해야 다양성 집계에 들어가고** 이 작품은 미등재다.

### 오판 ② — 레퍼런스 대신 사내 작품을 기준으로 삼았다 (2차)

사용자가 `/dash/d30`을 **예시로** 들자 나는 d30을 기준선으로 재제작했다. 셸·KPI 행·8/4는 생겼지만 액센트(rose)·활자(mono)·다크 히어로 밴드 부재로 **레퍼런스와는 여전히 다른 페이지**였다. 사용자가 같은 링크를 다시 주며 *"이번에는 이걸로"*.

**교훈 둘**:
1. 주문 제작에서 정본 조항으로 사용자를 설득할 때는 **그 조항의 적용 범위를 먼저 확인한다.** 루프 내부 규칙은 주문에 안 걸린다.
2. **레퍼런스가 있으면 레퍼런스가 기준이다.** 사용자가 든 사내 예시는 "이런 종류"를 가리킨 것이지 그것을 베끼라는 뜻이 아니었다.

### 3차에서 바꾼 방법

**designer에게 레퍼런스 이미지를 직접 읽혔다**(`.ref-tmp/`에 두고 첫 도구 호출에서 Read). 1·2차는 내가 말로 옮겼고, 그 번역에서 어긋났다.

## 2. 인터뷰 결과 (3판 공통)

| 묻는 말 | 답 |
|---|---|
| "딱 하나만 기억한다면?" | **돈이 어디서 와서 어디로 빠졌나** |
| "회사 이름과 언어는?" | **지어주세요 + 한/영 토글** → `Ledgerline` |
| 읽는 사람 | 사업주·재무 담당, 이미 고객이고 매주 연다 |

## 3. 축 (3차에서 전면 교체)

| 축 | 1·2차 | **3차** | 근거 |
|---|---|---|---|
| 액센트 | rose | **teal-900 / emerald-500** | 레퍼런스의 가장 결정적인 시각 특성 |
| 활자 | display-mono | **display-grotesk** | 레퍼런스 숫자는 지오메트릭 산세리프 |
| 테마 | light | light (배경은 순백이 아닌 옅은 회색) | 레퍼런스 |

rose·mono는 트리에 **한 글자도 없다.** 통화는 USD 유지 — 레퍼런스는 €지만 `data.ts`가 USD 센트 정수 기반이고, 요청은 형태에 대한 것이었다.

## 4. 레퍼런스 대조 (designer 보고)

| 레퍼런스 블록 | 구현 | 의도적 차이 |
|---|---|---|
| 사이드바 2구획 | GENERAL(인페이지 앵커 4) + **ACCOUNTS**(계좌 선택기 3) | SUPPORT의 `Capital/Vaults/Reports/Earn`이 **전부 죽은 링크**라, 같은 형태(아이콘+라벨+뱃지)에 실제로 동작하는 계좌 선택기를 넣었다. `Pro Mode` 스위치 자리는 **표 밀도를 실제로 바꾸는** `Compact rows`로 |
| 상단행 | 검색(실검색) + 날짜 칩 + 기간 `<select>` + **Export(실 CSV 다운로드)** | `⌘+F` 힌트 제거 — *"구현하지 않은 단축키를 표기하는 건 거짓말이다"* |
| 다크 히어로 밴드 | `teal-900` + 잔액 + 증감칩 + 액션 3개(활성 = `emerald-500` 채움) | Add/Send/Request를 **방향 필터**(All/Money in/Money out)로 — 레퍼런스의 "밝은 초록 1개 + 반투명 나머지" 형태는 그대로 두고 버튼이 실제로 표·차트·CSV를 바꾼다 |
| Cash Flow | 발산 막대(위 `teal-800` / 아래 `emerald-500`) + Money in/out 아이콘 타일 쌍 | 세그먼트를 Weekly가 아닌 **Grouped** — *"30일=3일 버킷, 90일=10일 버킷이라 주간이 거짓"*. `Manage` 제거(동작 없음) |
| stat 카드 3장 | 3계좌 + 순이동 칩 + `vs. … last period`, 카드 전체가 선택 버튼 | 증감을 %가 아닌 **부호 금액** — *"순이동은 음수가 되므로 이전 음수 대비 퍼센트가 방향을 뒤집는다"* |
| Recent Activity | 4열 시맨틱 표 + 원형 방향 아이콘 + 상태 pill + 마스킹 수단 | 보조 통화(`120 USD`) 자리를 **기간 합계 대비 비중**으로 — 데이터가 USD 단독이라 환산 통화가 없다 |
| My Cards | `teal-900` 카드(생성형 SVG) + 마스킹 번호 + 잔액 + 보유자 | VISA 로고는 자체 도형. `See All ↗` 자리에 **나머지 2계좌 목록**을 두고 누르면 그 카드가 앞으로 — *"전부 보기가 갈 곳이 없으니 같은 자리를 실제 전환으로"* |

브라우저 크롬·레퍼런스 인물명(`Young Alaska`)·이메일은 가져오지 않았다. 인물은 `Dana Whitlock / Treasury lead`이고 **이메일은 아예 만들지 않았다** — *"만들면 실재 주소와 충돌할 수 있어"*.

## 5. 게이트 — 9관문 1차 통과

```
route OK · types 0 · static 0 · lint 0 · weights 3종(렌더 실측)
sweep 전 폭 오버플로 0 · console 결함 0 · a11y 100 · perf 78
샷 16장(4폭 × 4스크롤), blank 0
```

1·2차와 달리 **1-fix 0회로 통과했다.** designer가 구조로 회피한 것:

| 위험 | 회피 |
|---|---|
| `fontFamily` 상수 추출 (`hopline` 27건) | 호출부마다 리터럴 반복 |
| `sr-only` × `overflow-x-auto` (`hopline` 248px) | **가로 스크롤 컨테이너를 트리에 두지 않고** 열을 브레이크포인트로 숨김 |
| UA 기본 bold가 4종째로 계측 | 전 `h1~h3`·`th`에 명시 클래스, `<strong>`·`<b>` 0개 |
| `react-hooks/set-state-in-effect` (2차 실패) | **`useEffect` 0개** — 파생은 전부 `useMemo`, 상태 변화는 이벤트 핸들러 |

`useEffect` 0건·비결정론 0건은 오케스트레이터가 실측 확인했다.

## 6. 실측 검증

**언어 토글**: 기본 한글 **0자**·`lang=en` → KO **718자**·`lang=ko` → EN 복귀 **0자**·`lang=en`. 혼용 없음.

**동기화**: 흐름 필터 `Money in` → 표 19→**8행**. 계좌 → Payroll 시 히어로 `$42,819.05`→**`$15,963.40`**, 표 6행. 정렬 컬럼 4개 `aria-sort` 시맨틱 확인.

**절단**: 1440·390 양쪽에서 실제 텍스트 절단 **0건**.

## 7. 판정 전 수정 1건

사이드바 언어 라벨 `Language`(필요 62px)가 49px 칸에서 `Lang…`으로 잘려 렌더됐다. 게이트에는 안 걸리지만(오버플로 아님, 전문이 DOM에 있어 a11y 통과) 미완성으로 읽혔다.

designer가 라벨을 `sr-only`로 돌리고 `EN`/`KO` 필을 우측 정렬했다. 폭을 깎는 안도 계산해 봤으나 *"슬랙 4px뿐이라 폰트 메트릭이 조금만 넓어져도 재발"*이라 버렸다. **그룹의 접근 이름은 `Language`로 유지**되고(`aria-labelledby` → `sr-only` span), 실측으로 확인했다.

## 8. designer가 정한 것 — 브리프에 없던 것 중 무게 있는 것

- **`h1`의 위치** — 사이드바 워드마크가 아니라 히어로 밴드의 `Available balance · Operating ····4417`. *"사이드바 워드마크를 `h1`으로 쓰면 DOM 순서상 헤딩이 본문보다 앞서 어긋난다."* 사이드바 구획 라벨은 헤딩이 아니라 `<p id>` + `aria-labelledby`로 처리해 레벨 스킵을 원천 차단
- **초록 채움 위 텍스트를 흰색이 아닌 `emerald-950`** — *"`emerald-500` 위 흰색은 약 2.3:1로 AA 미달"*
- **보조 텍스트를 전면 `zinc-600` 통일** — 표면이 흰색·`zinc-50`·`zinc-100`으로 섞여 있어 표면별 하한을 따지는 대신 가장 어두운 쪽에 맞췄다
- **차트를 SVG가 아니라 flex + height%** — *"뷰포트마다 종횡비가 달라 SVG는 좌표 재계산이나 왜곡을 부른다"*
- **차트 호버 툴팁 미구현** — *"일별 90막대에서 히트영역이 44px 미만이 되어 컨트롤 크기 규칙과 정면 충돌한다."* 대신 `role="img"` 요약 라벨 + **레퍼런스에 없는 범례**를 달아 색만으로 방향을 전달하지 않게 했다
- **계좌 선택기를 3곳에 중복 배치**(사이드바·stat 카드·My Cards) — *"한 상태를 여러 진입점에서 조작·확인할 수 있어 동기화가 눈에 보인다"*

## 9. 재사용

**`data.ts`는 세 판 내내 한 글자도 고치지 않았다.** 39건 거래·3계좌·`BASELINE`·이중언어 사전(`CATEGORY`/`METHOD`/`STATUS`/`UI`)·집계(`txnsFor`·`totalsOf`·`bucketsOf`)·포맷터·타입 전부 재사용. 미사용으로 남긴 것: `SCHEDULED`·`byCategory`·`byDay`·`deltaWord` — *"레퍼런스에 대응 블록이 없어 억지로 넣지 않았다."*

신규는 전부 셸 전용(`shell.ts`의 `SHELL` 사전 40여 개 + `compactMoney` + CSV 직렬화).

## 10. 폐기

2차(d30 골격) 서브트리 5파일(`LedgerlineApp.tsx`·`accounts-panel.tsx`·`flow-chart.tsx`·`StreamFeed.tsx`·`ui.tsx`)이 통째로 고아가 되어 오케스트레이터가 삭제했다 — **이번 변경이 만든 orphan**이라 정리했고 무관한 dead code는 건드리지 않았다. 1·2차 전체는 git 이력에 있다.

## 샷

`shots/` — 4폭(1280·1440·1920·390) × 4 스크롤 지점, 16장. 3차본으로 전량 교체.
