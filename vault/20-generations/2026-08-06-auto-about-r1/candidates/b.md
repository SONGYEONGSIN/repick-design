# Candidate b — about-evolve/r1/b

Tallwood (fictional observability/monitoring SaaS — "one place to see, understand and act on what your systems are doing") gets a grid-driven "team + culture directory" About page: hero mission statement, then a department-chip + live-search combo (single shared filter state) that recomputes one 15-person team grid of deterministic inline-SVG monogram avatars, each card a keyboard-operable disclosure (`aria-expanded`/`aria-controls`) that reveals a bio; an office-locations section with a list/map toggle where the map is a deterministic inline-SVG schematic grid (no external map embed); a static 4-pillar values section; and a careers mailto CTA. Light theme, teal-700 as the single sparing accent (solid fills use teal-700 rather than teal-600 to clear AA against white text), `--font-display-mono` for headline type only, exactly 3 font-weight classes (`font-normal`/`font-medium`/`font-bold`) across the route.
