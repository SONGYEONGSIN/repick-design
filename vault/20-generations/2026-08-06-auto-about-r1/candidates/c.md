# Candidate c — about

Northline, a fictional B2B sourcing platform ("sourcing that a retail buyer can actually audit"), told through a light/rose "process + impact" About page: a hero mission statement sits above an always-visible inline-SVG diagram of the Source→Verify→Match→Ship pipeline, a horizontal 4-step sequence below it drives that same diagram's node/segment highlight via shared React state on hover *and* keyboard focus (progress-fill styling: completed segments/nodes turn rose, current node is solid rose, upcoming stays neutral zinc), each step also has an independent "read the full story" expand/collapse panel, a static tabular-nums impact-stats band follows, then a deterministic partner-logo marquee (reusing the existing global `marquee` keyframe) with an explicit play/pause toggle that also pauses on hover/focus and disables under `prefers-reduced-motion`, and a mobile-only single-open accordion replaces the horizontal step row below the `md` breakpoint (tapping a row both opens it and syncs the hero diagram) before a rose careers CTA. Exactly 3 font-weight classes used route-wide (`font-medium`/`font-semibold`/`font-bold`), no display font, no dark theme.
