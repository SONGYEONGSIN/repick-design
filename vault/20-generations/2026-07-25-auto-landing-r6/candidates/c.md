Candidate C — "Instant Estimate Certificate": the hero puts three real controls directly in front of the user (category chips, a condition-grade chip row, and a target-price range input), and every change deterministically recomputes and count-up-animates a bordered, receipt-like "Estimate Certificate" card — complete with a serial ID (`RP-OUA-085`-style), an itemized base/condition/demand price breakdown, an AI fair-price figure, an estimated payout after fees, and a live match-score bar — so the payoff reads as a credible, screenshot-able document rather than a single recalculated number.
