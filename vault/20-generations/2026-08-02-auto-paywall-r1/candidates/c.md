c — Trestle (team whiteboard SaaS): a single-tier "hard paywall" interrupt — no 3-tier grid — pairing a workspace-specific limit-hit narrative (left) with an always-visible sticky upgrade panel (right) whose billing-period toggle and seat slider both live-recompute price, total, and the CTA's own label; below the fold, a locked-preview image + feature list, a real prev/next testimonial carousel, and a single-open FAQ accordion. Dark theme, amber accent, `--font-display-wide`, exactly 3 font weights.
