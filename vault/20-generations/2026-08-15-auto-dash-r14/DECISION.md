# auto-dash-r14 — DECISION

## 결과: **c (Crewline) 승리 — 3-0 만장일치**

| 렌즈 | 1위 | 2위 | 3위 |
|---|---|---|---|
| 렌즈1 (brief 준수) | c | a | b |
| 렌즈2 (상용 SaaS 완성도) | c | b | a |
| 렌즈3 (아키타입 차별성) | c | b | a |

c가 3렌즈 전부에서 1위 — 다수결/타이브레이크 규칙을 적용할 필요 없는 명확한 승리. no-winner 아님.

## 렌즈1 — 브리프 준수 (판정 전문)

**순위: c > a > b**

- **c (Crewline)**: 점검한 전 항목 클린 — 결함 없음.
- **a (Harborline)**: 셸/컴포넌트 시스템 우수하나, DetailPane이 `max-w-6xl`로 고정 캡을 둬 1920px에서 좌우 ~107-113px 여백이 남는 "조용한 캡" 위반(`DetailPane.tsx:88`) — 브리프의 "결과로 잰다" 규칙(1920에서 페이지 패딩만 남아야 함) 위반. `a-1920.png`에서 시각 확인, `a-1440.png`에서는 안 걸림(가용폭이 캡보다 좁아서).
- **b (Rampart)**: 컴포넌트 시스템 품질은 a/c와 동급이나, ⌘K 커맨드 팔레트 검색 input에 `outline-none`만 있고 `focus-visible:outline`도 `focus-within` 대체 표시도 전혀 없는 **죽은 포커스 관용구**(`command-palette.tsx:64,76`) — a(`CommandPalette.tsx:42`)·c(`command-palette.tsx:49`)는 동일 위치에 `FOCUS_WITHIN`을 정확히 적용했으나 b만 누락. 이 결함은 하드게이트 `focus` 관문이 **기본 렌더 뷰의 Tab 순서만** 스캔하므로 (팔레트는 사용자 액션 후에만 마운트) 자동 검증을 그대로 통과했다(a11y 100, focus 0건). 부차적으로 `reviewer-table.tsx:63-65`의 sr-only caption 래퍼에도 `position:relative` 앵커 누락(낮은 확신 — 390px overflow 실제 발현 여부 미확인).

## 렌즈2 — 상용 SaaS 완성도 (판정 전문)

**순위: c > b > a**

- **c**: 지배 시각화(주간 스케줄 그리드)가 최대치로 상시가독 — 모든 job 블록이 시간/장소를 hover 없이 직접 표기. 1280px에서 KPI 행 단일 줄 유지, 기술자명은 말줄임(...)으로 안전 처리. 뷰포트락 선택이 실제 상용 디스패치 툴(ServiceTitan류) 관행과 일치 — 정당한 커밋.
- **b**: 다크 테마 실행이 Coinbase/n8n급. Bullet 차트+미니차트 값 상시노출 충족. 스크롤 진행(top→mid→bottom)이 실제 서사(정렬 가능한 Reviewer capacity 표로 귀결) — 패딩성 스크롤 아님. 단, 1280px에서 "At a glance" 카드의 "ESCALATED"·"UNASSIGNED" 라벨이 gap 부족(`grid-cols-3 gap-2`)으로 시각적으로 붙어 보임(`side-panel.tsx:26,31`) — 델타 목록(r8 계열, 1280px 캡션 절단류)에 해당하는 재발 패턴.
- **a**: SLA 트렌드 카드의 헤드라인 값 자체("1h 3m")가 1280px에서 "1h"/"3m"으로 줄바꿈되고 트렌드 필 "vs. prior period"의 "period"가 두 번 고아 줄바꿈됨(`SlaTrendChart.tsx:66,70-74`, `ui.tsx:71-79`) — b의 캡션 결함보다 더 눈에 띄는 실패(헤드라인 숫자 자체가 깨짐). 우측 카드 스택 높이 불균형도 부차 관찰.

## 렌즈3 — 아키타입 차별성 (판정 전문)

**순위: c > b > a**

- **c**: 기술자×요일 실제 스케줄 그리드가 페이지를 지배 — d40(캘린더-히트맵을 KPI 위젯으로 내장)과 구조적으로 구분됨, d33(칸반)과도 구분. 지배 시각화(전체 캘린더 그리드)가 갤러리에 없는 유형. KPI를 인라인 `dl` 행으로 처리해 과사용된 "4카드 KPI 행"도 회피.
- **b**: 3열 구성(BulletGrid | ActivityFeed | AtAGlance+Trend)이 콘텐츠 층위에서는 진짜 신규(라이브 검수 피드 + Bullet 차트 — 갤러리 첫 사용)이나, 매크로 골격(좁은 플랭크 + 넓은 중앙 컬럼)이 가장 최근 갤러리 작품 d45(Portlane, r13 승자)의 3-페인 지오메트리와 기하학적으로 겹침 — 콘텐츠는 다르나 zone-ratio가 유사.
- **a**: master-detail 자체는 갤러리에 없는 신규 골격이나, 브리프가 스스로 "Linear/Superhuman식" 정석 예시로 드는 가장 범용적인 형태. 지배 시각화(라인/영역 SLA 트렌드)는 d31·d32와 같은 계열이고, 심지어 같은 라운드의 b가 부차 위젯(QueueTrendCard 스파크라인)으로 쓴 것과도 같은 차트 패밀리 — 레이어를 낮춰 재수렴(curation-criteria Q21/Q22와 같은 패턴, 순위엔 미반영·진단 정보로만 기록).

## 판정 커버리지 (자진 신고 종합)
- 세 렌즈 모두 1280/1366/1600px 실측 스크린샷 없이 1440px 근사 + 소스 계산으로 대체 판단(a/c는 뷰포트락이라 스크롤 프레임 자체가 없음 — 결함 아님).
- hover/포커스 렌더 상태, 모션 품질은 정적 스크린샷 한계로 미판정(자진 신고, 허위 판정 아님).
- b의 sr-only 앵커 결함은 390px 실제 overflow 발현을 스크린샷으로 확인 못해 낮은 확신으로 기록.
- 렌즈3은 d29-d44 전수 소스 대조 대신 works.ts desc 필드 + d40/d45 타깃 대조로 대체(예산 내).

## 재개/기권
재개 0회, 기권 0회 — 3렌즈 전부 1회 디스패치로 4요건(랭킹/사유/근거/커버리지 자진신고) 충족 완료.

## 정제 조치
없음 — a는 게이트 재실패(1-fix 루프, SCORES.md 기록) 이후 승자가 아니므로 판정 후 수정 대상 아님. 승자 c는 판정 전부터 결함 0건.
