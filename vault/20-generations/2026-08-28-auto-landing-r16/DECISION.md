# DECISION — auto-landing-r16

target: landing · round: auto-landing-r16 · date: 2026-08-28 · winner: **c ("AI Redline")** · no-winner: false

## Result

2:1 다수결. **렌즈1(정본 준수)·렌즈2(상용 완성도) 두 렌즈가 c를 1위**로, **렌즈3(아키타입 차별성)만 b를 1위**로 판정했다.

| 렌즈 | 1위 | 2위 | 3위 |
|---|---|---|---|
| 렌즈1 (DNA 준수) | **c** | a | b |
| 렌즈2 (상용 완성도) | **c** | b | a |
| 렌즈3 (아키타입 차별성) | b | **c** | a |

## 후보 요약 (블라인드 판정 — designer 정체 무관)

- **a — Quarterly Disclosure**: 재배정 큐(`auto-landing-r14/c`) 형태를 이번 라운드에 배정, 지정된 `aria-label` 수정 사항을 정확히 반영해 하드게이트 통과. 조작(감사 카테고리 선택)이 분기·해결율·전분기 대비를 실시간 재계산하고 클로징 CTA까지 결속 — "조작=가치체감" 자체는 만족. 그러나 **히어로 첫 폴드에 매물+증명이 없다**(제품 카드는 `VerifiedNow`라는 별도 섹션으로, 스크롤해야 등장) — 정본 §Landing구조 1번 정면 위반으로 렌즈1·렌즈2 모두 감점.
- **b — Comparables Radius**: 반경 슬라이더가 지도 위 매물 점·인레인지 카운트·가격밴드·상위매치 4장을 동시에 재계산 — 이번 라운드 렌즈3이 가장 신규로 판정한 입력×출력 조합(연속 슬라이더 × 극좌표 산점도 지도, 카탈로그에 전례 없음). 그러나 (1) a와 같은 방식으로 히어로 첫 폴드에 매물 카드의 증명 태그가 실제로는 안 보임(카드가 폴드 경계에 걸쳐 이미지만 보임) (2) **이미지가 깨져 보인다** — `images.unsplash.com` 핫링크가 이 샌드박스의 아웃바운드 프록시에서 차단돼 스크린샷에 깨진 이미지 아이콘으로 찍혔다. 렌즈1·렌즈2 모두 3위로 감점.
- **c — AI Redline**: 히어로 자체 안에 헤드라인+조작 컨트롤(카테고리 필터)+실시간 신뢰도 점수+매물 카드(94% match·B+ 등급·인증·−12%)가 **스크롤 없이 한 프레임에** 전부 들어온다 — 정본 §Landing구조 1번을 유일하게 완전 충족. 조작이 리드라인 텍스트·증거 아코디언·신뢰도 점수·클로징 CTA까지 4개 면을 동시 갱신하고, 기본 상태(전 카테고리 활성)가 이미 4/4 교정·100/100으로 "무조작 시 두 값이 같다"는 기존 결함 패턴을 명시적으로 회피(designer 자신이 `data.ts` 주석에 이 이유를 남김). 유일하게 사진을 전혀 안 쓰고(샌드박스 Unsplash 차단을 예견해 아이콘 스와치로 대체) 깨진 이미지 리스크 자체가 없다.

## 환경 아티팩트 정정 (사람 확인 없이 기록 — r12·r15 선례와 동일 패턴)

렌즈2는 b의 깨진 이미지를 "실제 렌더 결과"로 정당하게 감점했다(판정 시점의 화면이 그렇게 보였으므로). 다만 원인은 b의 코드 결함이 아니라 **이 샌드박스의 아웃바운드 프록시가 `images.unsplash.com`을 차단**하는 환경 제약이다 — `auto-landing-r12`·`auto-landing-r15`에서 이미 두 차례 같은 아티팩트가 기록됐다(`vault/30-ledger/auto-ledger.jsonl` 해당 라운드 note 참조). **판정 결과는 뒤집지 않는다** — c가 렌즈1·렌즈2 양쪽에서 이겼고, 그 근거의 다른 축(첫 폴드 위반)은 b·a 공통이라 이미지 문제와 무관하게도 c가 이겼다. 다만 b가 신은 손해를 다음 라운드가 "b의 코드 결함"으로 오독하지 않도록 여기 정정해 둔다.

## 정제 조치

없음 — 승격 감사(1-fix)는 §3 하드게이트 단계에서 이미 처리·재게이트 완료(전원 통과). 판정 후 추가 수정 없음.

## LEARN — DELTAS 반영

아래 delta를 `landing-deltas-provisional.jsonl`에 append(§5).
