# Candidate b — Portside careers page

Fictional B2B marketplace **Portside** (matches open trailer capacity with shippers in real time) careers page built around a light/orange, no-display-font, 3-weight (`font-normal`/`font-medium`/`font-bold`) system: hero mission statement, then an always-visible live-search combobox over 10 open roles (substring match on title/team/location, full arrow-key nav with `aria-activedescendant`, live `aria-live` result count, Enter/click expands the selected role in place with duties + a `mailto:` apply link scoped to that role's title), then a semantic `<table>` (caption, `scope="col"`/`scope="row"`, `aria-sort`) comparing 6 benefit categories across US-FT / Intl-FT / Contract tiers with real column-header sort (rank-based, icon direction, not color-only), then a 4-panel deterministic inline-SVG "Life at Portside" strip (remote-first, biannual Home Port meetup, Freight Days hack week, dockside standups), then a footer with a real `mailto:careers@portside.io` apply CTA.
