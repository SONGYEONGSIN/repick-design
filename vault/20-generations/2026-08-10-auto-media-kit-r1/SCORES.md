# SCORES — auto-media-kit-r1

동결 해시: `cb91576bcaad8cea17e228457f922d410a990c8b` · 전 후보 1차 통과(1-fix 불요)

## 하드게이트

| 후보 | route | static | lint | weights | sweep | a11y | perf |
|---|---|---|---|---|---|---|---|
| a | 응답 OK | 위반 0 | 0 | 3종 | 전 폭 오버플로 0 | 100 | 78 |
| b | 응답 OK | 위반 0 | 0 | 3종 | 전 폭 오버플로 0 | 100 | 78 |
| c | 응답 OK | 위반 0 | 0 | 3종 | 전 폭 오버플로 0 | 100 | 79 |

세 라운드의 게이트 학습을 designer 프롬프트에 직접 넣은 결과 — `font-normal` 명시(→ 셋 다 3종) · `key` 중복 주의(→ React 경고 0건, 수동 확인) · 라우트 200(→ 전원 통과).

스크린샷 48장, **blank 0/48.**

## 3렌즈 랭킹 — 첫 3파전 완전 동률

| 렌즈 | 1위 | 2위 | 3위 |
|---|---|---|---|
| 1 — 정본 준수 | a | c | b |
| 2 — 상용 완성도 | c | a | b |
| 3 — 아키타입 차별성 | b | c | a |

**tie-break**([[curation-criteria]] ②): brief 렌즈 1위 `a` → archetype 렌즈가 `a`를 최하위로 명시 → 제외 → 나머지 {b, c} 중 brief 1위 = **c**. 렌즈3이 c를 최하위로 찍지 않아(2위) 확정.
"차별성↔완성도 상충 시 완성도 다수결"도 같은 방향 — `b`는 두 완성도 렌즈에서 모두 최하위.

**승자 c.** 만장일치 3연속(contact·developers·integration) 뒤 첫 동률 라운드다.

## judge가 실측으로 잡은 데이터 결함

| 후보 | 결함 | 근거 |
|---|---|---|
| a | "FILES IN ARCHIVE **18**" / "eighteen files" vs `formats` 배열 합 **20** | `a/data.ts:102,115,128,141,154,167` |
| b | 룰02 "One ink per lockup … Nothing else" vs 룰04 "On light surfaces switch to the Ink cut" — 밝은 회색 위 Ink는 룰02가 금지한 조합 | `b/data.ts:72` vs `:144` |
| b | 타입 스펙이 내부 구현 누출 — `var(--font-display-mono)`가 화면에 노출, 서체명·라이선스 부재 | `b/data.ts:194-199` |

## 판정 커버리지 (자진 신고)

세 judge 모두 **중단 없이 완주**(15·15·15 호출)했고 미확인 범위를 스스로 밝혔다:

- 렌즈1 — 프레임 12장 + `page.tsx` 3개. **자식 컴포넌트 전량 미열람** → *"`CompareBand`에 강한 정적 폴백과 완전한 포커스/모션 처리가 실재하면 b는 c와 순위 교체 가능"* · c의 `KitBody`(컨트롤 ~20개) 미확인이라 *"c의 실질 a11y는 사실상 미평가"*
- 렌즈2 — 프레임 12장 + `data.ts` 3개. 인터랙션 미실행 · 390은 첫 폴드만 · *"a의 5열 테이블과 c의 3열 그리드가 390에서 어떻게 접히는지가 가장 큰 미확인 리스크"*
- 렌즈3 — 프레임 9장 + `page.tsx` 3개. **배정된 조작 어휘를 모른 채 판정**(블라인드) — *"'배정은 작동했다' 판단은 산출물 역추정"* · 모바일 미확인이라 *"b의 모바일 성립 실패 시 1위는 흔들릴 수 있다"*
