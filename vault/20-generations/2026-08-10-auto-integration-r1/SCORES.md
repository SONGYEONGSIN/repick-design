# SCORES — auto-integration-r1

판정 시점 동결 해시: `923fc570935c2ff5cc1ffce21774a46a51e68236`
게이트: `node scripts/gate.mjs --target web --routes /integration-evolve/r1/<v>` · 전 후보 1차 통과

## 하드게이트

| 후보 | route | static | lint | weights | sweep | a11y | perf |
|---|---|---|---|---|---|---|---|
| a | 응답 OK | 위반 0 | 0 | 3종 | 전 폭 오버플로 0 | 100 | 78 |
| b | 응답 OK | 위반 0 | 0 | 3종 | 전 폭 오버플로 0 | 96 | 78 |
| c | 응답 OK | 위반 0 | 0 | 2종* | 전 폭 오버플로 0 | 100 | 77 |

\* 계측 아티팩트. 명시 클래스가 `font-medium`·`font-semibold` 둘뿐이고 본문 400은 Tailwind preflight 기본값이라 카운터가 못 본다. 렌즈1이 프레임에서 렌더 3종을 육안 확정했다. `auto-careers-r1`과 같은 메커니즘(그때는 4종을 3종으로, 이번은 3종을 2종으로).

## 정제 조치 후 재게이트 (승자 b)

중복 키 해소 후: `route OK · static 0 · lint 0 · weights 3종 · sweep 0 · a11y 96 · perf 78`, 브라우저 `same key` 경고 **0건**. 렌더 출력 불변이라 캡처·판정 유효.

## 스크린샷

후보별 16장(4폭 × 4 스크롤), 총 48장. **blank 0/48.**

## 3렌즈 랭킹

| 렌즈 | 1위 | 2위 | 3위 |
|---|---|---|---|
| 1 — 정본 준수 | b | c | a |
| 2 — 상용 완성도 | b | a | c |
| 3 — 아키타입 차별성 | b | a | c |

**승자 b — 3:0 만장일치.** contact r1 · developers r1에 이어 3라운드 연속.

## 판정 커버리지 (자진 신고)

세 judge 모두 **중단 없이 완주**했고(12·15·15 호출) 미확인 범위를 스스로 밝혔다:

- 렌즈1 — 프레임 12장 + 각 후보 `page.tsx`만. **자식 컴포넌트 전량 미열람** → a의 테이블 `scope`·포커스·모션, c의 `GrantSheet` 시맨틱 미확인. *"a의 3위는 이 공란에 크게 의존하므로 `FieldMap.tsx`가 갖췄다면 a↔c는 실질 동률"*이라고 스스로 단서를 달았다
- 렌즈2 — 프레임 12장 + `data.ts` 3개. **`page.tsx` 미열람** · 상호작용 전량 미관찰(*"안 보였다가 없다는 아니다"*) · c의 `residency` 탭 구간이 프레임 사이로 빠져 *"이 구간이 인스턴스 데이터를 담고 있었다면 c 감점은 과할 수 있다"*
- 렌즈3 — 프레임 9장 + `page.tsx` 3개. **클라이언트 컴포넌트 전량 미열람** → *"전역 재계산 재발 여부는 코드로 확증하지 못했다"* · 인접 카탈로그 소스 미열람(지시 준수)
