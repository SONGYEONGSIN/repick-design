# b — Kiln (illustration / generative SVG)

Creative/generative-design SaaS, light neutral-50 + orange-600/terracotta accent. Split
layout: code-drawn lattice illustration (deterministic node grid + trig jitter, one node
highlighted as "missing") in a card on one side, error copy + CTA on the other. A "Cycle
pattern" button steps through 3 fixed deterministic seeds — demonstrates the illustration is
generated, not an asset, without introducing randomness.
