# DECISION — auto-landing-r20

Freeze-state hash (all 3 candidates, post 1-fix): `8669680704757a34e1b3511eaf87eb4163b343eb`

**편차 고지**: 이 세션의 Agent 서브에이전트는 이 레포에 `.claude/agents/designer.md`·`comparator.md` 커스텀 정의가 없어, designer·judge 역할 모두 `general-purpose` 타입으로 대체했다(도구 접근이 스킬이 가정하는 제한 세트보다 넓음 — Bash 포함). 블라인드·독립 전제(§0-0)는 유지된다: 3 designer는 서로의 산출물을 못 읽게 지시받았고, 3 judge는 서로의 판정·candidates/*.md(디자이너 자기평가)·다른 run의 DECISION을 못 읽게 지시받았으며, 실제로 그 경계를 지켰음을 각자 self-report에서 확인했다. `self_judged` 플래그는 불필요 — 생성과 판정이 서로 다른 세션에서 독립적으로 이뤄졌다.

## 후보 요약
- **a — Qualification Wizard**: 4단계 순차 위저드(카테고리→컨디션→브랜드등급→구입가)가 답변마다 지속-노출 추정 패널(범위·comps·확신도·근거문장)과 히어로 예시 카드(등급·매치%·가격·할인)를 동시 재계산. 접근성·엔지니어링은 탄탄하나 **히어로 컴포넌트 안에 증명 카드가 스크롤 없이 보이지 않음**(1440·390 양쪽에서 FeaturedCard가 폴드 아래로 밀림) — 정본 §Landing구조1 정면 위반.
- **b — Time-Scrubbed Ticker**: 고정 4단계 이력(등록→AI등급→오퍼수락→검증완료)을 가로 스크러버(`role=slider`, 키보드 지원)로 드래그하면 가격·할인율·상태칩·내러티브 캡션·마일스톤 체크리스트가 동시 보간·재계산. 클로징 CTA가 같은 파생값을 그대로 인용.
- **c — Bundle Builder**: 6개 매물 중 몇 개든 추가/제거하는 가변 카디널리티 다중선택 토글이 번들 할인 곡선(6개 tier 막대, 현재 tier 강조)과 트러스트 롤업(가중평균 + 항목별 기여막대, 추가/제거마다 애니메이션)을 동시 재계산. 클로징 CTA가 4개 파생값 전부 인용.

## 판정
- **렌즈1(브리프/DNA 준수)**: b 1위 · c 2위 · a 3위. 결정 요인은 정본 §Landing구조1 — b·c는 1440에서 증명 카드가 스크롤 없이 완전히 보이지만, a는 FeaturedCard가 폴드 아래로 밀려 하드 위반("무엇을 더 잘했든 이 규칙 위반은 그것과 무관하게 감점한다"는 정본 문언 그대로 적용).
- **렌즈2(상용 SaaS 완성도)**: c 1위 · b 2위 · a 3위. c는 6항목 조합형 토글이 두 독립 표면(할인곡선+트러스트롤업)을 동시 재계산하는 상호작용 밀도가 가장 높고, 실제 사진(next/image, 고정 aspect-ratio)을 쓴 반면 a는 페이지 전체에 사진이 0장(전부 아이콘 타일)이라 "시제품처럼 읽힌다"고 명시 감점. a의 390px 카테고리 버튼 라벨 잘림(`truncate`)도 지적.
- **렌즈3(아키타입 차별성)**: c 1위 · b 2위 · a 3위. a는 기존 카탈로그 v16(Price Band — 형질 확인이 추정 범위를 좁힘)의 스킨 변형으로 판정(입력 UI만 순차 위저드로 다름, 출력 형태 동일). b는 v8(단일 슬라이더→다이얼)·v14(스크롤 타임라인)와 인접하지만 "고정 내러티브 타임라인을 비디오처럼 스크럽"하는 진짜 재조합으로 인정. c는 **가변 카디널리티 다중선택 세트빌더**가 카탈로그 전체(연속 슬라이더 기반 v15·v21, 이산 단일선택 v20·r19)에 없는 새 입력 축이라고 판정 — 가장 강한 차별성.

## 집계
1위 표: **c 2표(렌즈2·렌즈3) vs b 1표(렌즈1)** — 2:1 다수결로 **c 승리**. no-winner 표 0건(3렌즈 전원 승자 지정). 완전 동률 아님(tie-break 예외 미해당).

a는 3렌즈 전원 3위(만장일치 최하위) — 정본 §Landing구조1 위반이 세 렌즈 모두에서 독립적으로 감점 사유로 인용됨(렌즈1은 직접, 렌즈2·3은 "제품 사진 0장"·"기존 아키타입 스킨 변형"이라는 별개 사유로도 동시에 최하위).

## 정제 조치
없음 — 승자(c)는 1차 하드게이트에서 이미 전 항목 통과(a11y 95, 승격된 7종 하드페일 감사 무결점). §3-1 판정 후 수정 대상 없음.

## LEARN
델타 1건 적재 — 아래 참조. 재현 주장 없음(신규 관찰, L1).

## 정본 무변경 확인
`vault/00-principles/dash-brief-v3.md`·`design-principles.md` 무변경. `/dash` 갤러리·`/v1~v5` 무변경. `landing-deltas-provisional.jsonl`은 append만.
