# auto-dash-r18 — DECISION

target: dash · round: auto-dash-r18 · date: 2026-08-24
후보 source hash(1-fix 후 최종 동결): `507dfefbfe287c5c343a84675c8f30ce167a5063`

## 후보

- **a — Quorum** (`app/src/app/dash-evolve/r18/a/`): Trust & Safety 심사 콘솔. 매크로 골격 = 피드 중심(좌우 보조 페인 없음, sticky 요약 스트립 + 세로 결정 스트림). 인터랙션 문법 = "결정하면 큐가 전진한다"(카드 접힘 → 카운터 재계산 → 다음 미결 건 자동 포커스).
- **b — Tenure** (`app/src/app/dash-evolve/r18/b/`): B2B 구독 갱신 데스크. 매크로 골격 = 마스터-디테일(좌 계약 레일 + 우 상세 페인). 인터랙션 문법 = 상세 페인 내부 독립 "시점 스크러버"(vantage) — 레일을 안 건드리고 상세 안의 4개 지표·타임라인·티켓 원장이 재계산.
- **c — Trellis** (`app/src/app/dash-evolve/r18/c/`, **승자**): SaaS 리텐션 분석 콘솔. 매크로 골격 = 매트릭스 우선(코호트 삼각행렬이 콘텐츠 영역 대부분, 행/열 주변합 부착). 인터랙션 문법 = "기준 행 고정(pin-as-baseline)" — 고정 시 격자 전체 인코딩이 절대값(순차 램프)에서 기준 대비 델타(발산 램프)로 전환.

## 하드게이트

3후보 전원 1-fix 후 10/10 통과. 상세는 SCORES.md.

**3/3 재현 — 동일 결함 클래스**: 세 후보 전원이 ⌘K 팔레트가 열릴 때 검색어를 초기화하는 로직을 `useEffect` 안에서 `setState`로 처리해 `react-hooks/set-state-in-effect` 하드페일을 독립적으로 발생시켰다(a: `CommandPalette.tsx`+`QueueConsole.tsx` 2건, b: `RenewalDeskClient.tsx` 1건, c: `CommandPalette.tsx` 2건). 전부 "렌더 중 상태 조정" 패턴(`if (open !== wasOpen) { setWasOpen(open); if(open) setQuery(""); }`)으로 전환해 해소하고, DOM `.focus()` 호출만 별도 effect에 남겼다. a는 추가로 포커스 요청 함수가 `commands` 배열(→ JSX prop으로 렌더에 흘러 들어감)에 담긴 함수에서 ref를 직접 건드려 `react-hooks/refs`(렌더 중 ref 접근) 파생 위반 4건을 냈다 — ref 접근을 effect 안으로만 격리하고 트리거를 순수 state(함수형 업데이트)로 바꿔 해소.

환경 고유 절차(스킬 밖): Lighthouse가 `CHROME_PATH`를 요구한다(`PW_CHROMIUM_PATH`만으로는 `unavailable`) — 이 세션은 Bash 호출 간 env가 유지되지 않아 인라인 export가 매번 필요했다.

## JUDGE 패널 — 2:1 다수결 (동률 아님, tie-break 불요)

| 렌즈 | 1위 | 2위 | 3위 |
|---|---|---|---|
| 렌즈1 (브리프 준수) | **b** | a | c |
| 렌즈2 (상용 SaaS 완성도) | **c** | b | a |
| 렌즈3 (아키타입 차별성) | **c** | a | b |

1위 표: c 2표(렌즈2·렌즈3), b 1표(렌즈1). **다수결로 승자 = c (Trellis)**.

## 승자 사유 요약

- 렌즈2(1위): 격자 모든 셀이 hover 없이 숫자를 상시 인쇄해 "단일 지배 시각화 즉시가독" 요건을 문자 그대로 초과 충족. 핀 인터랙션이 장식이 아니라 인코딩 자체(순차↔발산 램프, 라벨, 마진, 커브 도메인)를 재작성. 대비값을 실측 주석으로 문서화(`encoding.ts` — orange-700 3.42:1 실패 사례 직접 언급하며 회피). 데이터 정합성이 구조적으로 강제(`cohort-data.ts` 코호트 규모=세그먼트 합). 모바일은 표 압축이 아니라 근본적으로 다른 스택 카드 레이아웃으로 재설계.
- 렌즈3(1위): 삼각형 행렬 자체가 카탈로그의 직사각형 히트맵(d30·d40)·트리맵(d35)과 형태적으로 겹치지 않음. **이번 라운드 핵심 함정(selectedId 다중위젯 동기화 수렴)을 코드 주석으로 명시 반증**하고 실제 props 대조로 확인됨 — `baselineId`의 유일한 소비자는 `buildEncoding` 하나뿐, `SegmentPanel`은 baseline을 아예 모름.
- 렌즈1(2위 — 근소한 감점 사유): "선택→다중위젯 동기화"를 브리프가 예시로 든 형태(여러 형제 위젯이 하나의 선택에 동시 반응)로 **의도적으로 구현하지 않았다** — c의 핀은 격자 자신만 바꾸고 세그먼트 패널·근거 카드는 핀을 모른다. 렌즈1은 이를 인터랙션 4종 체크리스트 미달 위험으로 읽었다(3종은 확실, 4종째는 해석에 의존). **이 감점 사유가 정확히 렌즈2·3이 c를 1위로 만든 바로 그 설계 결정과 같은 지점**이라는 점이 흥미롭다 — 아래 LEARN 델타 참조.

## 탈락 후보 사유

- **b (렌즈1 1위, 다수결 2위)**: "선택→다중위젯 동기화"를 브리프가 예시로 든 형태 그대로 가장 교과서적으로 구현(레일 선택 또는 vantage 스크러버 이동 → 4개 지표+이벤트창+티켓원장+차트커서 동시 재계산) — 렌즈1이 이를 최우선으로 평가. 그러나 렌즈2·3은 마스터-디테일이 SaaS에서 가장 관습적인 골격이고, 콤보 차트(영역+선+막대)가 카탈로그 d44(개인 자산 현금흐름 차트)와 형태적으로 인접하다고 지적해 차별성에서 밀림.
- **a (전 렌즈 2위 — 매우 근소)**: 접근성 엔지니어링(결정 시 포커스 자동 이동, `aria-live` 상시 announce)이 세 후보 중 가장 정교하다고 전 렌즈가 언급. 그러나 "선택→다중위젯 동기화"에 해당하는 뚜렷한 패턴이 약하고(카드 펼침이 상단 카운터만 갱신), 지배 시각화(카드 내 소형 영역차트)가 상대적으로 평범하다는 지적으로 2위에 머묾. 결정적 탈락 사유는 없음 — 순수 상대 비교.

## 정제 조치 — 없음

승자 c는 3렌즈 전원이 하드게이트급 규칙 위반을 지적하지 않았다. 판정 후 수정 없이 그대로 승격 후보로 유지.

## 미확인 범위 (판정 커버리지)

- 렌즈1: 1920px 실측은 b만 확인(a·c는 코드 역산). `CommandPalette.tsx`(a·c)·`CohortStack.tsx`(c 모바일) 미열람.
- 렌즈2: gate.mjs 직접 미실행. b 하단 스크롤 콘텐츠(뷰포트 완결형으로 안내받아) 코드로만 확인.
- 렌즈3: `b/data.ts` 원본 미열람(간접 추론). 기존 카탈로그(d29~d49) 소스 미열람(한 줄 요약만 사용). 태블릿 폭(1024~1279px) 스크린샷 미제공.
- 3렌즈 공통: 런타임 인터랙션(클릭·키보드) 직접 실행 없이 소스+스크린샷 정독으로 판정.

## 재개·기권

3렌즈 전원 1회 완주, 재개·기권 없음.

---

## 승격 판정 (2026-08-26 주간 apply)

**승자 `c` 는 승격하지 않는다 — 형태만 다음 라운드 재배정 대상으로 남긴다.**

승격 직전 검사(스킬 §4-1 신설분)에서 **카피가 전량 한글**로 확인됐다 — `c` 폴더 기준 한글 1754자, 9개 파일 187줄. [[page-brief-repo]] §1 *"카피 언어: 영문 전용 — 한글·혼합 금지"* 위반이다. **세 후보 전원이 같은 상태였다**(page.tsx 기준 a 43 · b 68 · c 75자).

- **원인은 후보가 아니라 오케스트레이션이다.** `dash-evolve` §1 RETRIEVE 목록에 `page-brief-repo` 가 없어 designer 가 이 규칙을 본 적이 없다. 같은 apply 에서 스킬을 고쳤다(§1·§2 양쪽).
- **왜 고쳐서 올리지 않았나.** §3-1(판정 후 수정)은 규칙 위반 해소만 허용하고 *"순위를 바꿀 만한 수정은 재판정 사유"* 라고 못박는다. 이 카피는 스크린리더 `detail`·`readout` 문자열까지 한글 문법의 보간 템플릿으로 짜여 있어, 번역이 곧 **렌즈1·2 가 평가한 접근성 문장 전체의 사후 저작**이 된다.
- **함께 남은 것**: 웨이트 4종(SCORES 기록 — a 4종 · b 3종 · c 4종).
- **재배정 배정문에 넣을 것**: "코호트 삼각행렬 + 기준행 고정이 격자 자신의 인코딩(색 램프·라벨 단위·마진·도메인)을 재작성하는 형태를, **영문 카피로**." 형태는 이 라운드의 값어치이고 탈락 사유가 아니다.
- **delta 는 별개로 보류됐다** — `r17/c ↔ r18/c` L2 승격은 [[questions-queue]] Q46(브리프 문구 ↔ 렌즈 해석 충돌) 답변 전까지 유예.
